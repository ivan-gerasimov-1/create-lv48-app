## 1. Web template baseline

- [x] 1.1 Update `templates/base/apps/web/package.json` and related Vite wiring for Tailwind CSS `v4`
- [x] 1.2 Add the web template files needed for Tailwind `v4` global styling and shadcn-ready setup (`components.json`, aliases, utility helper, baseline styles)
- [x] 1.3 Refresh the generated `apps/web` starter UI and README so they demonstrate the new Tailwind + shadcn-ready baseline

## 2. Scaffold and config alignment

- [x] 2.1 Update any scaffold-sensitive config or metadata needed so the new `apps/web` files and aliases are generated correctly
- [x] 2.2 Keep placeholder interpolation and package naming behavior intact for the expanded web template assets

## 3. Verification

- [x] 3.1 Extend scaffold smoke verification to assert Tailwind `v4` dependencies, generated styling files, and shadcn-ready wiring in `apps/web`
- [x] 3.2 Run the affected test/build commands and fix any verification gaps before handoff
