## 1. Web template baseline

- [ ] 1.1 Update `templates/base/apps/web/package.json` and related Vite wiring for Tailwind CSS `v4`
- [ ] 1.2 Add the web template files needed for Tailwind `v4` global styling and shadcn-ready setup (`components.json`, aliases, utility helper, baseline styles)
- [ ] 1.3 Refresh the generated `apps/web` starter UI and README so they demonstrate the new Tailwind + shadcn-ready baseline

## 2. Scaffold and config alignment

- [ ] 2.1 Update any scaffold-sensitive config or metadata needed so the new `apps/web` files and aliases are generated correctly
- [ ] 2.2 Keep placeholder interpolation and package naming behavior intact for the expanded web template assets

## 3. Verification

- [ ] 3.1 Extend scaffold smoke verification to assert Tailwind `v4` dependencies, generated styling files, and shadcn-ready wiring in `apps/web`
- [ ] 3.2 Run the affected test/build commands and fix any verification gaps before handoff
