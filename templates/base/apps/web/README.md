# Web App

React + Vite app for {{displayName}} with a starter setup for Tailwind CSS and shadcn/ui.

## Included UI baseline

- Tailwind CSS (PostCSS + config)
- shadcn-compatible `components.json`
- `cn()` utility in `src/lib/utils.ts`
- starter `Button` component in `src/components/ui/button.tsx`
